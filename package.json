{
  "name": "cosmos",
  "version": "1.0.0",
  "engines": {
    "node": "22.14.0"
  },
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install --include=dev --legacy-peer-deps && npm run build"
  }
} 